import {Dict, Int, Str} from "../../../lib";

describe("the dict field", () => {
    let field: Dict;

    beforeEach(() => {
        field = new Dict(null, new Str(null), new Int(null));
    })

    it("should load data", () => {

    });

    it("should dump data", () => {

    });
});
